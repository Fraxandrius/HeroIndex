// Firebase will be configured in a later milestone.
// Keep this file side-effect free until credentials and products are selected.
export const firebaseClient = null

export function getFirebaseClient() {
  return firebaseClient
}
